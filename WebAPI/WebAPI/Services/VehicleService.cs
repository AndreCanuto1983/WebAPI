using System.Threading.Tasks;
using Teste.Models;
using WebAPI.FrontModels;
using WebAPI.Models;

namespace WebAPI.Services
{
    public class VehicleService
    {
        public async Task<VehicleModel> CudVehicle(VehicleModel model)
        {
            //CrudBaseService crud = new CrudBaseService(ApplicationDbContext, model);

            //using (var context = new ApplicationDbContext())
            //{
            //}'

            //return count;
        }
    }
}